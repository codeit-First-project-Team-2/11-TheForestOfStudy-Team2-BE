# 환경 변수 예시 파일

# 로컬 postgres에 study_forest_dev DB 생성
# .env.example 복사 -> .env.development 파일 생성 (username, password 수정)
# npm prisma:migrate 실행

# 환경 설정
NODE_ENV=development

# 서버 포트
PORT=4000

# PostgreSQL 연결 URL
DATABASE_URL="postgresql://postgres:11511@localhost:5432/study_forest_dev"